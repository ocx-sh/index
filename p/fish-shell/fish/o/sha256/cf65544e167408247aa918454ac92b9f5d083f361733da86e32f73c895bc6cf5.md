# fish

fish is a smart and user-friendly command line shell for Linux, macOS, and the rest of the family. It works out of the box with autosuggestions from your history, syntax highlighting as you type, and rich tab completions derived from man pages — no configuration required. Its scripting language is consistent and readable, with built-ins like `math` and `string` for everyday shell work.

## What's included

- **fish** — the fish command line shell (interactive prompt and `fish -c "..."` script runner)

On macOS the upstream archive additionally ships **fish_indent** (the script formatter) and **fish_key_reader** (the terminal key-sequence inspector); the Linux archive carries the `fish` binary alone.

## Links

- [fish Documentation](https://fishshell.com/docs/current/)
- [fish website](https://fishshell.com)
- [fish on GitHub](https://github.com/fish-shell/fish-shell)

## License & source

The fish binaries redistributed here are licensed **GPL-2.0-only** (© Axel
Liljencrantz and the fish-shell contributors). The complete Corresponding Source
for each mirrored version is available from the upstream tagged tree — e.g.
version `X.Y.Z` at <https://github.com/fish-shell/fish-shell/releases/tag/X.Y.Z>,
or `git clone https://github.com/fish-shell/fish-shell && git checkout X.Y.Z`.
A 3-year written offer for the source is in `NOTICE.md`. The OCX mirror pipeline
files are Apache-2.0.
